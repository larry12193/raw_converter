cmake_minimum_required(VERSION 2.8.3)
project(see3cam_cu51_driver)

## Compile as C++11, supported in ROS Kinetic and newer
# add_compile_options(-std=c++11)

include_directories(
  ${catkin_INCLUDE_DIRS}
  /home/lawrence/CMU/research/cuberover/raw_converter/dependencies/opencv_econ/include
)

find_package(catkin REQUIRED COMPONENTS
  roscpp
  rospy
  std_msgs
  cv_bridge
)

set(CMAKE_PREFIX_PATH "/home/lawrence/CMU/research/cuberover/raw_converter/dependencies/opencv_econ")
link_directories(/home/lawrence/CMU/research/cuberover/raw_converter/dependencies/opencv_econ/lib)

catkin_package()

add_executable(see3cam_cu51_node src/see3cam_cu51_driver.cpp)

target_link_libraries(see3cam_cu51_node
                      ${catkin_LIBRARIES})

install(TARGETS see3cam_cu51_node
  ARCHIVE DESTINATION ${CATKIN_PACKAGE_LIB_DESTINATION}
  LIBRARY DESTINATION ${CATKIN_PACKAGE_LIB_DESTINATION}
  RUNTIME DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
)
